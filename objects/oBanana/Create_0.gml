// Inherit the parent event
event_inherited();

image_speed = 5/room_speed;
//set the movement rules for this projectile (might actually be done in enemy)

// set type for interaction with other objects
type = "projectile";